---
description: Una regla del catálogo - enunciado, racional, capa de cumplimiento (solo lectura)
---
Sigue el protocolo de `.claude/skills/lashary-contexto/SKILL.md`. Regla: $ARGUMENTS

Lee su entrada en `docs/spec/rules.yaml` y su sección completa en el doc que el campo `doc`+`anchor` señala. Reporta: enunciado, racional, severidad, capas de cumplimiento y estado del check de CI (implementado / planificado_f6 / no_aplica). Si el ID no existe, dilo — no inventes reglas.
