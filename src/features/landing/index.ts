// Entry point público de la feature landing (ARCH-003): lo único importable desde afuera.
// Contenido editable vía la feature `content` (ADR-0001).

export { HomePage } from './ui/HomePage'
export { FALLBACK_HOME_CONTENT } from './constants/fallback-home-content'
